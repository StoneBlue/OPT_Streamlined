KERBALCHANGELOG //Required to have this name
{
	showChangelog = True
	modName = OPT Spaceplane Continued
	
	VERSION
	{
		version = 3.4.9
		CHANGE
		{
			type = Add
			change = Added wrapper solar panels.
			change = Added wrapper radiators.
		}
		CHANGE
		{
			type = Change
			change = Updated dry masses on all parts that hold fuel. They should meet the CryoTanks standard.
			change = Updated localization on parts that have cargo doors, ramps, docking ports and ladders.
		}
		CHANGE
		{
			type = Change
			change = Updated engines: 
			subchange = WarpJets are now single-mode Pulsed Inductive Thruster (still thrusty like we like it).
			subchange = Dark Drive is now a Plasma Core Antimatter engine. (Dark Goo and Black Hole stuff are gone now.)
			subchange = ARI-75, J-92 Nebula are no longer "SCOOP" hybrid plasma voodoo.
		}
		CHANGE
		{
			type = Fix
			change = Fixed (killed) 1km spawn kraken in J Deployment Bay, J Large docking Port.
		}
		CHANGE
		{
			type = Remove
			change = Removed radiator module from fuselages and wings.
		}
	}
	
	VERSION
	{
		version = 3.1.1
		change = Forgot to update version number...
	}
	
	VERSION
	{
		version = 3.1
		change = Added missing science module to K lab, without which, science operations and transmission were broken.
	}
	
	VERSION
	{
		version = 3.0
		change = Added Localization support.
		change = Fixed bulkheadprofile in Nebula engine. (Spotted by @ Lisias.)
		change = Fixed bulkheadprofile in some I and J parts.
		change = Fixed K Drone Core's SAS module still missing.
		change = Fixed K and KH cargo bays and hollow fuselages appearing in Fuel Tank category (moved to Payload).
		change = Fixed missing bulkheadprofile in RCS ports.
		change = Increased thrust in these engines:
		change = change = Valkyrie tilt & WarpJet MARGE.
		change = change = VTOL wrapper engines.
		change = Moved the following in from OPT Legacy:
		change = change = 2.5m Nebula Shock Intake.
		change = change = VTOL Wrapper engines.
		change = change = Avatar tilt and WarpJet MARGE engine.
		change = change = DepthMask assets.
		change = change = K cockpits (custom, TAV). All 3 K cockpits are now here.
		change = change = K cabin and lab.
		change = change = K and KH Inline SAS.
		change = Removed obolste parts: some RCS thrusters, some Avatar fuselage parts. Craft files that have these will break.
		change = Reduced node size on H parts.
		change = Updated DepthMask patch to detect standalone DepthMask plugin.
	}
	
	VERSION
	{
		version = 2.1.0
		change = Deleted antiquated plugin bundles.
		change = Deleted MM patches folder. Salvaged configs moved to OPT Reconfig.
		change = Deleted flags. They are replaced and moved to OPT Reconfig.
		change = Updated Agency logos and moved to OPT Reconfig.
	}
}